my_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres_container
      port: 5432
      user: db_user
      password: db_password
      dbname: db
      schema: dev
      threads: 1